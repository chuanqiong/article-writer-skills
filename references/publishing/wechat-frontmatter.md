# WeChat Frontmatter

- 位置: HTML 开头的 `<!-- ... -->` 注释块中，内部使用 YAML frontmatter。
- 必填字段:
  - `title`: 最多 64 字
  - `author`
  - `digest`: 建议 50-120 字
  - `cover`: 封面图路径
- 推荐字段: `tags`, `summary`, `date`, `wordCount`
- 封面图建议: `900x500`, `jpg/png`, 本地或在线路径可读
- 其他图片如果超过 2MB，先压缩再推送
